import { css } from 'aphrodite/no-important';
import * as React from 'react';
import styles from './styles';

export default function Product ({  }) {
  return (
    <section className={css(styles.Product)}>
    </section>
  );
};